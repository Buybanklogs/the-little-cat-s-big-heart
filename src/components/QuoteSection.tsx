const QuoteSection = () => {
  return (
    <section className="bg-background py-16 md:py-20 paw-watermark">
      <div className="container mx-auto px-6 max-w-3xl text-center">
        <blockquote className="text-2xl md:text-3xl font-heading italic text-primary leading-relaxed">
          "Sometimes we only discover who we truly are when someone shows us kindness."
        </blockquote>
      </div>
    </section>
  );
};

export default QuoteSection;
